<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="text">
      <h3> {{comment_text}} </h3>
    </div>
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
  </section>
  {{/each}}
</div>